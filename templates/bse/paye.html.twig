{% extends 'base.html.twig' %}

{% block body %}
    <h1>Paye Bse</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Payer</button>
    {{ form_end(form) }}
    <a href="{{ path('bse_index') }}">Back to list</a>
{% endblock %}
