<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Acceuil</title>
    <link rel="stylesheet" href="{{ asset('assets/w3.css') }}">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="{{ asset('assets/bootstrap/bootstrap.min.css') }}" rel="stylesheet">
    <script src="{{ asset('assets/bootstrap/bootstrap.bundle.min.js') }}"></script><!-- Font Awesome CDN -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

    <style>
            *{
                box-sizing: border-box;
            }
            body{
                align-items: center;
                display: flex;
                flex-direction:column;
                background: #f6f5f7;
                font-family:'monserrat', sans-serif;
                min-height: 100%;
                margin: 5%;
            }
            a{
                text-decoration: none;
                color: grey;
            }
            body, h1, h2, h3, h4, h5, h6 {
                font-family: "Times New Roman", Times, serif;
            }
            .w3-display-container {
                position: relative;  /* Nécessaire pour que le bouton soit correctement positionné */
            }

            .w3-display-topright {
                position: absolute;
                top: 0;
                right: 0;
                z-index: 1000;  /* S'assurer que le bouton est au-dessus des autres éléments */
            }

            .fa-times {
                pointer-events: auto;  /* Assurez-vous que le bouton est cliquable */
            }
            #voir{
                float: left;
            }


    </style>
</head>
<body>
    <div class=" w3-center w3-animate-zoom w3-half w3-card-4 w3-white">
        <div class="w3-display-container w3-yellow">
            <a href="{{path('user_index')}}" class="fa fa-times w3-display-topright"></a>
            <h4  class="w3-opacity w3-padding-16">{{ user.titre }} {{ user.nom|upper }} {{ user.prenom }}</h4>
        </div>

        <div class="w3-row-padding w3-margin-bottom w3-opacity w3-half" id="voir">
            <p><i class="fas fa-id-badge"></i> Matricule: {{ user.matricule }}</p>
            <p>
                {% if user.sexe == 'femme' %}
                    <i class="fas fa-female"></i>Sexe: {{ user.sexe }}
                {% elseif user.sexe == 'homme' %}
                    <i class="fas fa-male"></i>Sexe: {{ user.sexe }}
                {% endif %}
            </p>
            <p><i class="fas fa-briefcase"></i> Fonction: {{ user.fonction }}</p>
            <p><i class="fas fa-heart"></i> Situation Familiale: {{ user.situationFamiliale }}</p>
            <p><i class="fas fa-id-card"></i> CIN: {{ user.cin }}</p>
            <p><i class="fas fa-calendar-alt"></i> Date CIN: {{ user.dateCin|date('Y-m-d') }}</p>
            <p><i class="fas fa-money-bill-wave"></i> Taux journalier: {{ user.tauxJournalier }}</p>
            <p><i class="fas fa-money-bill-wave"></i> Adresse: {{ user.address }}</p>
            <p><i class="fas fa-money-bill-wave"></i> Nombre d'enfant: {{ user.nombreEnfant }}</p>
            <p><i class="fas fa-money-bill-wave"></i> Adresse: {{ user.address }}</p>

        </div>
        <div class="w3-half">
            <p>
                {% if user.sexe == 'femme' %}
                    <i class="fas fa-user"></i>{{ user.sexe }}
                {% elseif user.sexe == 'homme' %}
                    <i class="fas fa-user"></i>{{ user.sexe }}
                {% endif %}
            </p>
        </div>
            <a href="{{ path('user_edit', {id: user.id}) }}" class="w3-btn w3-indigo w3-half">Edit</a>
            <a href="{{ path('user_index') }}" class="w3-btn w3-indigo w3-half">Back to list</a>
    </div>
<body>