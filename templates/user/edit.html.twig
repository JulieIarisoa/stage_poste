<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Acceuil</title>
    <link rel="stylesheet" href="{{ asset('assets/w3.css') }}">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="{{ asset('assets/bootstrap/bootstrap.min.css') }}" rel="stylesheet">
    <script src="{{ asset('assets/bootstrap/bootstrap.bundle.min.js') }}"></script><!-- Font Awesome CDN -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

    <style>
            *{
                box-sizing: border-box;
            }
            body{
                align-items: center;
                display: flex;
                flex-direction:column;
                background: #f6f5f7;
                font-family:'monserrat', sans-serif;
                min-height: 100%;
                margin: 5%;
            }
            a{
                text-decoration: none;
                color: grey;
            }
            body, h1, h2, h3, h4, h5, h6 {
                font-family: "Times New Roman", Times, serif;
            }
            .w3-display-container {
                position: relative;  /* Nécessaire pour que le bouton soit correctement positionné */
            }

            .w3-display-topright {
                position: absolute;
                top: 0;
                right: 0;
                z-index: 1000;  /* S'assurer que le bouton est au-dessus des autres éléments */
            }

            .fa-times {
                pointer-events: auto;  /* Assurez-vous que le bouton est cliquable */
            }



    </style>
</head>
<body>
    <div class=" w3-center w3-animate-zoom w3-half w3-card-4 w3-white">
        <div class="w3-display-container w3-amber">
            <a href="{{path('user_index')}}" class="fa fa-times w3-display-topright"></a>
            <h4  class="w3-opacity w3-padding-16"><i class="fa fa-user-plus w3-opacity"></i>    Edit User</h4>
        </div>

        {# Démarrer le formulaire avec une classe CSS pour la mise en page #}
        {{ form_start(utilisateur) }}

            {# Champ de formulaire pour le matricule, le nom et prenom avec personnalisation de l'étiquette et des attributs #}
            <div class="w3-row-padding w3-margin-bottom w3-opacity">
                <div class="w3-third">
                    {{ form_row(utilisateur.matricule, { 'label': 'Numéro matricule', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
                <div class="w3-third">
                    {{ form_row(utilisateur.nom, { 'label': 'Nom', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
                <div class="w3-third">
                    {{ form_row(utilisateur.prenom, { 'label': 'Prénom', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
            </div>


            {# Champ de formulaire pour le fonction, l'email et taux journalier avec personnalisation de l'étiquette et des attributs #}
            <div class="w3-row-padding w3-margin-bottom w3-opacity">
                <div class="w3-third">
                    {{ form_row(utilisateur.fonction, { 'label': 'Fonction', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
                <div class="w3-third">
                    {{ form_row(utilisateur.email, { 'label': 'Email', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
                <div class="w3-third">
                    {{ form_row(utilisateur.taux_journalier, { 'label': 'Taux journalier', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
            </div>


            {# Champ de formulaire pour le titre, la situation familiale et le sexe avec personnalisation de l'étiquette et des attributs #}
            <div class="w3-row-padding w3-margin-bottom w3-opacity">
                <div class="w3-third">
                    {{ form_row(utilisateur.titre, { 'label': 'Titre', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
                <div class="w3-third">
                    {{ form_row(utilisateur.situation_familiale, { 'label': 'Situation familiale', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
                <div class="w3-third">
                    {{ form_row(utilisateur.sexe, { 'label': 'Sexe', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
            </div>


            {# Champ de formulaire pour le date cin et numéro cin avec personnalisation de l'étiquette et des attributs #}
            <div class="w3-row-padding w3-margin-bottom w3-opacity">
                <div class="w3-half">
                    {{ form_row(utilisateur.date_cin, { 'label': 'Date CIN', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
                <div class="w3-half">
                    {{ form_row(utilisateur.cin, { 'label': 'Numéro CIN', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
            </div>

            <!-- Fifth Row: address and nombre enfant -->
            <div class="w3-row-padding w3-margin-bottom w3-opacity">
                <div class="w3-half">
                    {{ form_row(utilisateur.address, { 'label': 'Addresse', 'attr': {'class': 'w3-input w3-border'} }) }}
                </div>
                <div class="w3-half">
                    {{ form_row(utilisateur.nombre_enfant, { 'label': 'Nombre d\'enfant','attr': {'class': 'w3-input w3-border'} }) }}
                </div>
            </div>


            {# Bouton de soumission du formulaire et bouton de retour avec un style #}
            <button type="submit" class="w3-btn w3-green w3-half">Modifier</button>
            <a href="{{ path('user_index') }}" class="w3-btn w3-blue w3-half">
                <i class="fa fa-arrow-left"></i> Retourner 
            </a>

        {# Fermer le formulaire #}
        {{ form_end(utilisateur) }}
    </div>
</body>
