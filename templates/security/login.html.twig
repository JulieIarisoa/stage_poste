<!DOCTYPE html>
<html>
<head>
        <title>Acceuil</title>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="{{ asset('assets/w3.css')}}">
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
        <link href="{{ asset('assets/bootstrap/bootstrap.min.css') }}" rel="stylesheet">
        <script src="{{ asset('assets/bootstrap/bootstrap.bundle.min.js') }}"></script>
        <style>
            *{
                box-sizing: border-box;
            }
            body{
                align-items: center;
                display: flex;
                flex-direction:column;
                font-family:'monserrat', sans-serif;
                margin: 5%;
            }
            body, h1, h2, h3, h4, h5, h6 {
                font-family: "Times New Roman", Times, serif;
            }
            .container{
                background:#fff;
                overflow: hidden;
                box-shadow: 0 14px 28px rgba(0,0,0,0.25);
                display: flex;
            }
            label{
                float: left;
                margin-top: 5%;
            }
            h1{
                font-weight: bold;
                margin: 0;
            }
            .logo-paositra{
                width: 150px;
                margin-bottom: 100px;
                margin-right: 80px;
                margin-left: 80px;
                margin-top: 20px;
            }
            button{
                padding: 8px 15px;
                border: 1px solid yellow;
                outline: none;
                margin: 3%;
                transition: transform 80ms ease-in;
                cursor: pointer;
            }
            button:active{
                transform: scale(0.90);
            }
            .logo{
                width: 8%;
                margin-right: 4%;
                margin-top: 2%;
            }
            @media(max-width:1050px){
                .logo-paositra, .info{
                    display: none;
                }
            }
        </style>
    </head>
</head>
<body>

<!-- Side Navigation -->
<nav class="w3-sidebar w3-bar-block w3-card w3-animate-left w3-center" style="display:none" id="mySidebar">
  <button class="w3-bar-item w3-button" onclick="w3_close()">Close <i class="fa fa-remove"></i></button>
</nav>

<!-- Header -->
            <img src="{{ asset('images/logo.png') }}" alt="logo-paositra-malagasy" class="w3-display-topright logo">
<header class="w3-container w3-theme w3-padding" id="myHeader">
  <i onclick="w3_open()" class="fa fa-bars w3-large w3-button w3-theme"></i> 
  <div class="w3-center">
  <h4 class=" w3-text-amber">GESTION AVANCES AUTORISEES</h4>
  <h1 class="w3-xxxlarge w3-animate-bottom w3-text-dark-gray">DIRPM FIANARANTSOA</h1>
  <p class=" w3-text-black">Direction Inter-Régional de la Paositra Malagasy Fianarantsoa</p>
    <div class="w3-padding-32">
      <button class="w3-button w3-white w3-hover-amber w3-hover-text-white w3-text-amber w3-border w3-border-amber w3-round-large" onclick="document.getElementById('id01').style.display='block'" style="font-weight:900;">SE CONNECTER</button>
    </div>
  </div>
</header>

<!-- Modal -->
<div id="id01" class="w3-modal">
    <div class="w3-display-topmiddle w3-card-4 w3-white w3-animate-top w3-half  w3-border w3-round-large">
      <header class="w3-container w3-theme-l1"> 
        <span onclick="document.getElementById('id01').style.display='none'"
        class="w3-button w3-display-topright">×</span>
        <div class="w3-container w3-cell w3-mobile w3-margin">
            <img src="{{ asset('images/logo.png') }}" alt="logo-paositra-malagasy" class="logo-paositra">
        </div>
        <div class="w3-cell w3-mobile">
            <form method="post">
                <h2 class="h4 mb-3 font-weight-normal w3-center w3-text-indigo w3-margin">Connexion</h2>
                {% if error %}
                    <div class="w3-text-red">Mot de passe ou email incorrecte{#{ error.messageKey|trans(error.messageData, 'security') }#}</div>
                {% endif %}

                <label id="label1">Email:</label>
                <input type="email" id="input1" placeholder="Entrer votre email" value="{{ last_username }}" name="email" class="w3-input" autocomplete="email" required autofocus  onfocus="handleFocus('input1', 'label1')" onblur="handleBlur('input1', 'label1')">

                <label id="label2">Password:</label>
                <input type="password"id="input2" name="password" placeholder= "Entrer votre mot de passe" class="w3-input" autocomplete="current-password" required onfocus="handleFocus('input2', 'label2')" onblur="handleBlur('input2', 'label2')">

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" >

                <div class="w3-margin">
                    <a href="{{path('app_forgot_password_request')}}">Mot de pass oublié?</a>
                </div>
                <button type="submit" class="w3-button w3-white w3-hover-indigo w3-hover-text-white w3-text-indigo w3-border w3-border-indigo w3-round-large w3-right" >Se connecter</button>
            </form>
        </div>
    </div>
</div>


