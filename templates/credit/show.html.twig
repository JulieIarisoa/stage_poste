<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Acceuil</title>
    <link rel="stylesheet" href="{{ asset('assets/w3.css') }}">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="{{ asset('assets/bootstrap/bootstrap.min.css') }}" rel="stylesheet">
    <script src="{{ asset('assets/bootstrap/bootstrap.bundle.min.js') }}"></script><!-- Font Awesome CDN -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

    <style>
            *{
                box-sizing: border-box;
            }
            body{
                align-bses: center;
                display: flex;
                background: #f6f5f7;
                font-family:'monserrat', sans-serif;
                min-height: 100%;
            }
            a{
                text-decoration: none;
                color: grey;
            }
            body, h1, h2, h3, h4, h5, h6 {
                font-family: "Times New Roman", Times, serif;
            }
            .w3-display-topright {
                position: absolute;
                top: 0;
                right: 0;
                z-index: 1000;  /* S'assurer que le bouton est au-dessus des autres éléments */
            }

            .fa-times {
                pointer-events: auto;  /* Assurez-vous que le bouton est cliquable */
            }

            .afficher_pas{
                display: none;
            }


    </style>
</head>
<body class="w3-light-grey">

    <div class=" w3-display-topmiddle w3-card-4 w3-white w3-animate-top w3-third w3-padding">
        <a href="{{ path('credit_index', {matricule: app.user.matricule}) }}">
            <span class="w3-button w3-display-topright">&times;</span>
        </a>
        <h4  class="w3-text-amber w3-center w3-padding w3-margin-bottom"><i class="fas fa-wallet"></i>   Solde
        </h4>

        <p>Date de renouvellement: {{ credit.dateRenouvellement|date('Y-m-d') }}</p>
        <p>Solde: {{ credit.creditInitial }} Ariary</p>
        {% for item in user %} 
            <p>Détenteur: {{ item.titre }} {{ item.nom | upper }} {{ item.prenom }}</p>
            <p>Numéro CIN: {{ item.cin }}</p>
            <p>Email: {{ item.email }}</p>
            <p>Fonction/indice: {{ item.fonction }}</p>
            <p>Adresse: {{ item.address }}</p>
        {% endfor %}
        <a href="{{ path('credit_edit', {id: credit.id}) }}" class="w3-button w3-white w3-hover-amber w3-hover-text-white w3-text-amber w3-border w3-border-amber w3-round-large w3-third w3-margin w3-right">Edit</a>
